{
  "mcpServers": {
    "nuxt": {
      "type": "url",
      "url": "https://nuxt.com/mcp"
    },
    "nuxt-ui": {
      "type": "url",
      "url": "https://ui.nuxt.com/mcp"
    }
  },
  "commands": {
    "api-client": {
      "description": "Regenerate the typed API client",
      "prompt": "Regenerate and verify the generated API client for this project.\n\n1. Run `bun run api-client:generate`.\n2. Do not hand-edit generated `*.gen.ts` files.\n3. If generation fails, diagnose whether the local backend or OpenAPI endpoint is the issue.\n4. Summarize the outcome and any follow-up needed."
    },
    "verify": {
      "description": "Run the repo verification flow",
      "prompt": "Run the standard verification flow for this Nuxt 4 + Bun project.\n\n1. Run `bun test`.\n2. Run `bun run build`.\n3. Summarize any failures or warnings.\n4. If this command was triggered after a code change, fix issues that are clearly caused by that change before summarizing."
    },
    "dev": {
      "description": "Start or inspect the dev setup",
      "prompt": "Work on the local development setup for this project.\n\n1. If needed, install dependencies with `bun install`.\n2. Use `bun run dev` for the dev server on port `12153`.\n3. If there is a dev-server issue, inspect the Nuxt config, runtime env, and Bun-specific wiring before making changes.\n4. Summarize what you started, changed, or found."
    }
  },
  "fileScan": {
    "exclude": [
      ".git/**",
      "node_modules/**",
      ".nuxt/**",
      ".output/**",
      ".nitro/**",
      ".data/**",
      ".cache/**",
      "dist/**"
    ]
  }
}
